git intro 
provided some basic commands to practise in git ll 
1.touch = to create new file 
2.cat >file name = to add date to the file 
3.cat file name = to read the date of the file 
git hub.com ---new repo's -- name of project -- description--- security--create repo's 
 git inti 
 git add file name
 git commit -m "comment of the proect "
 git add origin link from the git hub repo's page 
 git push origin master
 git status - to check the status of the file 
 ll - to know the how many files
 ls -la to know the hidden files 


1.when the files in local folder is called local repo's 
2.when files is commited is called staging 
3. when the files in the git then called as central repo's 